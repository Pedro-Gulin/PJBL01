# Copie este arquivo para server/.env e preencha com os seus dados.
# O arquivo .env NAO deve ser versionado (ja esta no .gitignore).

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=sua_senha_do_mysql
DB_NAME=bandas

PORT=5000
